# Decisions — Molly's feedback ledger

One dated file per decision: which variant won, what got rejected, WHY.
Captured by `fatbeats-content-learn` after every review round. Durable rules
graduate into `knowledge/editing-rules.md`; format quirks into
`knowledge/release-types.md`.

Format: `YYYY-MM-DD-{slug}.md` with sections **Decision / Why / Rule?**
